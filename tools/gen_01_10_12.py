# -*- coding: utf-8 -*-
"""単元01(最初のタスクループ) / 10(Skill化) / 12(総合演習) の素材を生成する。"""
import datetime as dt
import random
from pathlib import Path

from openpyxl import Workbook
from openpyxl.styles import Alignment, Font, PatternFill
from openpyxl.utils import get_column_letter

ROOT = Path(__file__).resolve().parents[1]
HEAD = PatternFill("solid", fgColor="1F3864")
HF = Font(bold=True, color="FFFFFF")
random.seed(11012)


def styled(ws, cols, widths):
    ws.append(cols)
    for c in ws[1]:
        c.fill, c.font = HEAD, HF
        c.alignment = Alignment(horizontal="center", wrap_text=True)
    for j, w in enumerate(widths, start=1):
        ws.column_dimensions[get_column_letter(j)].width = w
    ws.freeze_panes = "A2"


# ======================================================================
# 単元01: セットアップと最初のタスクループ
# ======================================================================
DAILY = {
"日報_田中_0901-0905.txt": """9/1(月)
 午前 定例。ミナト製作所の更新交渉、値引き3%の要望を共有した。
 午後 アオイ物流 訪問。倉庫の現場を見せてもらう。紙の帳票がまだ多い。
      ROI試算を作り直すことになった。次回 9/9。
9/2(火)
 サクラフーズ 見積提示。エンタープライズ 1,200万円 → 先方の予算枠は900万円らしい。
 プラン構成の見直しが必要。
9/3(水)
 社内。ミナトの値引きをプライシング会議に上申。結論は9/8。
 東雲テクノロジー から問い合わせ。API連携について。CSの伊藤さんに引き継ぎ。
9/4(木)
 ノースゲート商事 追加ライセンス50 → 受注。契約書送付済み。金額 600万円。
9/5(金)
 週次のパイプライン更新。フジノ精機が最終交渉に進んだ（720万円）。
 来週は火曜にサクラフーズ再提案。
""",
"日報_伊藤_0901-0905.txt": """■9/1
 カワセミ電機 定着支援キックオフ。管理者3名参加。現場への展開が課題。
 レポート機能の使い方が伝わっていない → 操作動画を作ることに。
■9/2
 東雲テクノロジー 問い合わせ対応（田中さんから引き継ぎ）。
 Factory Nexus 連携のバッチ間隔について。15分では業務が回らないとのこと。
 開発にリアルタイム同期の可否を確認 → 現状は難しいと回答。要注意。
■9/3
 九重ホールディングス QBR。利用率は高い。追加部署への展開を検討中とのこと。
 アップセルの芽あり。田中さんに共有した。
■9/4
 ひかり調剤 ログインできないとの連絡。SSO設定の変更が原因だった。1時間で解決。
■9/5
 CS定例。P1チケットの滞留が2件。エスカレーション基準を見直したい。
 ハルカゼ運輸、3ヶ月ログインなし。解約リスク高。来週アプローチする。
""",
"日報_佐藤_0901-0905.txt": """9/1 パートナー定例（日商システムズ）。案件は3件で停滞中。
9/2 みらい教育出版 初回商談。予算感は年200万円。スタンダードで十分そう。
9/3 パートナー向け勉強会の資料作成。
9/4 セイリング化成、代理店経由で見積依頼。プロプラン 30ライセンス。
9/5 トライアングル設計 失注。競合（他社CRM）に決まったとのこと。
    理由は価格。先方の予算は当初から厳しかった。
""",
}

NUMBERS_CSV = """指標,今週,前週,目標
新規受注額(円),13200000,9600000,8000000
新規商談数(件),7,9,8
失注額(円),3600000,0,
未解決P1(件),2,1,0
初回応答時間中央値(時間),7.8,5.0,4.0
"""

FIRST_TASK = """# 今週の週報（提出先: 営業本部長 大河内）

## 決まっている形式
- A4 1枚相当（Markdown で 60 行以内）
- 章立て: 「今週の結論」「数字」「動いた案件」「来週やること」「相談事項」
- 数字は必ず `data/今週の数字.csv` の値を使う（日報の記憶ベースの数字は使わない）
- 案件は金額の大きい順に最大5件
- 「相談事項」は 3 件まで。誰の判断が必要かを明記する

## 出力先
`units/01-setup-first-loop/output/週報_2026-W36.md`
"""


def unit01():
    base = ROOT / "units/01-setup-first-loop/data"
    base.mkdir(parents=True, exist_ok=True)
    for name, body in DAILY.items():
        (base / name).write_text(body, encoding="utf-8")
    (base / "今週の数字.csv").write_text(NUMBERS_CSV, encoding="utf-8")
    (base / "週報の書式.md").write_text(FIRST_TASK, encoding="utf-8")
    print("wrote unit01 materials")


# ======================================================================
# 単元10: 業務のSkill化
# ======================================================================
def monthly_report(month_label, mrr, mrr_prev, new_c, churn_c, cust, arpu,
                   frt, p1, highlights, risks, actions):
    growth = (mrr - mrr_prev) / mrr_prev * 100
    return f"""# Altair CRM 月次レポート {month_label}

作成: 経営企画部 / 提出先: 経営会議（第2火曜）

## 1. サマリ
- MRR は {mrr:,}円（前月比 {growth:+.1f}%）。
- 新規 {new_c} 社 / 解約 {churn_c} 社、期末顧客数 {cust} 社。
- ARPU {arpu:,}円。初回応答時間の中央値 {frt} 時間、未解決P1 {p1} 件。

## 2. 数字
| 指標 | 今月 | 前月 | 増減 |
|---|---|---|---|
| MRR | {mrr:,}円 | {mrr_prev:,}円 | {mrr - mrr_prev:+,}円 |
| 新規顧客 | {new_c}社 | ― | ― |
| 解約顧客 | {churn_c}社 | ― | ― |
| 期末顧客数 | {cust}社 | {cust - new_c + churn_c}社 | {new_c - churn_c:+}社 |
| ARPU | {arpu:,}円 | ― | ― |
| 初回応答時間(中央値) | {frt}時間 | ― | ― |

## 3. 今月のハイライト
{chr(10).join('- ' + h for h in highlights)}

## 4. リスクと課題
{chr(10).join(f'{i}. {r}' for i, r in enumerate(risks, 1))}

## 5. 来月のアクション
| # | やること | 担当 | 期限 |
|---|---|---|---|
{chr(10).join(f'| {i} | {a[0]} | {a[1]} | {a[2]} |' for i, a in enumerate(actions, 1))}

---
※ 数字はすべて当社調べ。実績値であり見込値は含まない。
"""


def unit10():
    base = ROOT / "units/10-custom-skill/data"
    (base / "過去の月次レポート").mkdir(parents=True, exist_ok=True)
    (base / "今月の素材_2026-09").mkdir(parents=True, exist_ok=True)

    specs = [
        ("2026年6月", 33_600_000, 32_900_000, 24, 8, 412, 81_553, 5.2, 0,
         ["ノースゲート商事の全社展開が完了（150ライセンス）",
          "Factory Nexus 連携がGA。既存15社が利用開始",
          "パートナー経由の新規が過去最高の9社"],
         ["解約8社のうち5社が『社内で使われなかった』を理由に挙げている",
          "CACが3ヶ月連続で上昇。直販チャネルの効率が悪化している"],
         [("定着支援プログラムの対象を利用率50%未満の全社に拡大", "伊藤", "7/31"),
          ("直販のリード獲得単価を分解して報告", "高橋", "7/15")]),
        ("2026年7月", 34_100_000, 33_600_000, 21, 11, 422, 80_805, 6.1, 1,
         ["定着支援プログラムを28社に展開。うち19社で週次ログイン率が改善",
          "みらい教育出版・ひかり調剤の2社をアップセル（合計 480万円）"],
         ["解約が11社と増加。うち4社が競合へ移行しており、価格が理由",
          "初回応答時間が6.1時間に悪化。P1が1件滞留している",
          "エンタープライズ商談の平均リードタイムが5.2ヶ月に長期化"],
         [("競合4社との価格・機能比較表を作成し営業へ配布", "田中", "8/8"),
          ("サポート体制を1名増員し、P1のエスカレーション基準を改定", "鈴木", "8/15"),
          ("エンタープライズ商談の停滞要因を案件別に棚卸し", "大河内", "8/22")]),
        ("2026年8月", 33_900_000, 34_100_000, 16, 14, 424, 79_953, 7.4, 2,
         ["SaaS Growth Conference で登壇。リード142件を獲得",
          "九重ホールディングスが追加2部署への展開を検討開始"],
         ["MRRが前月比マイナスに転じた。夏枯れに加え、解約14社が効いている",
          "初回応答時間が7.4時間まで悪化。増員は9月から",
          "『機能不足(レポート)』を理由とする解約が3ヶ月連続で発生している"],
         [("レポート機能の改善要望を集約し、開発ロードマップへ反映", "佐野", "9/12"),
          ("9月入社のサポート2名のオンボーディングを完了", "鈴木", "9/19"),
          ("解約企業14社へのエグジットインタビューを実施", "伊藤", "9/26")]),
    ]
    for label, *args in specs:
        fname = label.replace("年", "-").replace("月", "") + ".md"
        fname = fname.replace("-6", "-06").replace("-7", "-07").replace("-8", "-08")
        (base / "過去の月次レポート" / f"月次レポート_{fname}").write_text(
            monthly_report(label, *args), encoding="utf-8")

    # 9月分の素材（レポート本体はまだ無い＝これを作らせる）
    src = base / "今月の素材_2026-09"
    (src / "kpi_2026-09.csv").write_text(
        "指標,2026-09,2026-08\n"
        "MRR(円),35200000,33900000\n"
        "新規顧客数,29,16\n"
        "解約顧客数,10,14\n"
        "期末顧客数,443,424\n"
        "ARPU(円),79458,79953\n"
        "初回応答時間中央値(時間),4.6,7.4\n"
        "未解決P1(件),0,2\n"
        "NPS,31,27\n", encoding="utf-8")
    (src / "営業ハイライト.md").write_text(
        "- SaaS Growth Conference 経由のリード142件から、9月中に商談化したのは38件。\n"
        "  うち受注8件（合計 2,880万円）。イベント投資の回収見込みは3.1ヶ月。\n"
        "- 九重ホールディングス、追加2部署への展開が確定（+80ライセンス / 年960万円）。\n"
        "- サクラフーズはプラン構成を見直し、エンタープライズ→プロ＋アドオンで再提案。予算枠に収まった。\n"
        "- トライアングル設計は失注（競合CRM）。理由は価格。\n", encoding="utf-8")
    (src / "サポート状況.md").write_text(
        "- 9月からサポート2名増員。初回応答時間の中央値は 7.4h → 4.6h に改善。\n"
        "- 未解決P1はゼロ。8月から持ち越した2件はいずれも9/8までに解決。\n"
        "- 問い合わせカテゴリの内訳: レポート表示 31%、API連携 22%、SSO 14%、請求 11%、その他 22%。\n"
        "- レポート表示の問い合わせは3ヶ月連続で最多。開発ロードマップに改修が入った（10月着手予定）。\n", encoding="utf-8")
    (src / "解約とエグジットインタビュー.md").write_text(
        "解約10社。8月の14社から改善。\n\n"
        "| 顧客 | 理由 | コメント |\n|---|---|---|\n"
        "| ハルカゼ運輸 | 社内で使われなかった | 導入時の推進担当が異動し、後任が決まらなかった |\n"
        "| オリオン印刷 | 価格が高い | 事業縮小に伴う一律コスト削減 |\n"
        "| みなみ野スポーツ | 機能不足(レポート) | 店舗別の集計が標準でできない点が最後まで解消せず |\n"
        "| ユメミヤ商店 | 価格が高い | 競合の見積が約6割 |\n"
        "| （他6社） | 事業縮小/予算削減 4社、競合へ移行 2社 | ― |\n\n"
        "エグジットインタビュー（8月解約分14社）の所見:\n"
        "- 「使われなかった」群は、いずれも**導入後90日以内に管理者以外のログインが月2回未満**だった。\n"
        "  → 定着支援の起動条件を『90日時点のログイン率』に変えるべき。\n", encoding="utf-8")
    print("wrote unit10 materials")


# ======================================================================
# 単元12: 総合演習（QBR資料をサブエージェントで並列生成）
# ======================================================================
QBR_CLIENTS = [
    ("ミナト製作所", "プロプラン", 30, 10_800_000, "製造", 74),
    ("九重ホールディングス", "エンタープライズ", 150, 24_000_000, "持株", 88),
    ("ハルカゼ運輸", "プロプラン", 20, 7_200_000, "物流", 31),
]


def unit12():
    base = ROOT / "units/12-subagents-capstone/data"
    base.mkdir(parents=True, exist_ok=True)

    for name, plan, lic, amount, industry, score in QBR_CLIENTS:
        d = base / name
        d.mkdir(exist_ok=True)

        # 利用状況（週次12週分）
        lines = ["週,アクティブユーザー数,ログイン回数,レポート閲覧数,API呼び出し数,新規作成レコード数"]
        au = int(lic * (score / 100) * random.uniform(0.8, 1.0))
        for w in range(25, 37):
            drift = 1 + (0.01 if score > 60 else -0.03) * (w - 25)
            au_w = max(1, int(au * drift * random.uniform(0.92, 1.08)))
            lines.append(f"2026-W{w},{au_w},{au_w * random.randint(6, 18)},"
                         f"{au_w * random.randint(1, 9)},"
                         f"{random.randint(0, 40000) if score > 60 else random.randint(0, 800)},"
                         f"{au_w * random.randint(2, 25)}")
        (d / "利用状況_直近12週.csv").write_text("\n".join(lines) + "\n", encoding="utf-8")

        # サポート履歴
        cats = ["レポート表示", "API連携", "SSO", "請求", "モバイル", "データ移行"]
        sl = ["チケットID,起票日,カテゴリ,優先度,件名,ステータス,解決までの時間(時間),顧客満足度(1-5)"]
        for i in range(random.randint(4, 12)):
            day = dt.date(2026, 7, 1) + dt.timedelta(days=random.randint(0, 75))
            sl.append(f"TCK-Q3-{name[:2]}{i:02d},{day},{random.choice(cats)},"
                      f"{random.choice(['P1','P2','P2','P3','P3'])},"
                      f"{random.choice(cats)}について,"
                      f"{random.choice(['解決済','解決済','解決済','対応中'])},"
                      f"{random.randint(2, 96)},{random.randint(2, 5) if score > 60 else random.randint(1, 4)}")
        (d / "サポート履歴_Q3.csv").write_text("\n".join(sl) + "\n", encoding="utf-8")

        # 前回QBR議事録
        (d / "前回QBR議事録_2026-06.md").write_text(f"""# {name} 様 QBR 議事録（2026年6月）

- 日時: 2026年6月{random.randint(10, 25)}日 14:00-15:00
- 先方: 情報システム部 {random.choice(['佐々木','近藤','横山'])}様 ほか2名
- 当社: 田中（営業）、伊藤（CS）

## 先方からのフィードバック
- {'レポート機能は経営会議でそのまま使えており、助かっている。' if score > 60 else '正直、現場での利用が広がっていない。入力が二重になっている。'}
- {'API連携で基幹への転記作業がなくなった。月40時間の削減。' if score > 60 else '導入時の推進担当が異動し、後任が決まっていない。'}
- モバイルからの入力がしづらいという声が現場から出ている。

## 合意した宿題（当社側）
1. モバイル入力画面の改善要望を開発へ連携する（担当: 伊藤 / 期限: 7月末）
2. {'追加部署への展開に向けた見積を提示する（担当: 田中 / 期限: 7月中旬）' if score > 60 else '定着支援プログラムの再実施を提案する（担当: 伊藤 / 期限: 7月中旬）'}
3. 四半期ごとの利用状況レポートを定型化して送付する（担当: 伊藤 / 期限: 8月末）

## 次回
2026年10月上旬を予定。
""", encoding="utf-8")

    # 契約サマリ
    wb = Workbook()
    ws = wb.active
    ws.title = "契約サマリ"
    styled(ws, ["顧客名", "プラン", "ライセンス数", "年額(税抜)", "業種",
                "利用状況スコア", "契約開始", "更新期限", "営業担当", "CS担当"],
           [26, 18, 13, 16, 10, 15, 14, 14, 14, 14])
    for name, plan, lic, amount, industry, score in QBR_CLIENTS:
        ws.append([name, plan, lic, amount, industry, score,
                   "2024-10-01", "2026-10-15", "田中 健", "伊藤 さくら"])
    p = base / "契約サマリ.xlsx"
    wb.save(p)

    (base / "QBR資料の要件.md").write_text("""# QBR（四半期レビュー）資料の要件

顧客ごとに **3点セット** を作る。3社分あるので並列で進めてよい。

## 成果物（1社あたり）
| # | ファイル | 形式 | 内容 |
|---|---|---|---|
| 1 | `output/<顧客名>/QBR_2026Q3.pptx` | PowerPoint 6枚 | 表紙 / 利用状況 / サポート / 前回宿題 / 課題と提案 / 次のアクション |
| 2 | `output/<顧客名>/利用状況分析.xlsx` | Excel | 12週の推移、前四半期比、グラフ1つ以上 |
| 3 | `output/<顧客名>/社内申し送り.md` | Markdown | 営業向け。更新見込み、アップセル/解約リスク、根拠となる数字 |

## 必ず守ること
- 前回QBRの宿題3件について、**それぞれ完了/未完了と現状**を書く。書けない場合は「確認中」と明記する。
- 利用状況スコアが 40 未満の顧客には、アップセル提案を **入れない**。定着の話を先にする。
- 顧客に見せる資料（pptx/xlsx）に、他社名・社内の推測・解約リスクの記述を **入れない**。
  それらは `社内申し送り.md` 側にだけ書く。
- 数字は `data/<顧客名>/` のCSVから計算する。丸めるときは根拠を残す。
- 断定できないことは「確認中」と書く。推測で埋めない。

## レビュー観点（提出前に自分でチェック）
1. 3社とも6枚構成になっているか
2. 顧客向け資料に社内情報が混入していないか
3. 前回宿題3件すべてに言及があるか
4. スコア40未満の顧客にアップセル提案が入っていないか
5. すべての数字が data/ のCSVから再現できるか
""", encoding="utf-8")
    print("wrote unit12 materials")


if __name__ == "__main__":
    unit01()
    unit10()
    unit12()
