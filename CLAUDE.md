# このリポジトリで作業するときのルール

Claude Code 版「Cowork ユースケース」学習教材。
Claude Academy の *Introduction to Claude Cowork* が扱う業務ユースケースを、
Claude Code（ターミナル / リポジトリ / ブラウザ）で実践できる形に置き換えた14単元。

## ディレクトリ

| パス | 役割 | 書き込み |
|---|---|---|
| `units/<単元>/README.md` | 演習の指示書。学習目標・手順・プロンプト・合格チェック | 教材の改訂時のみ |
| `units/<単元>/data/` | 演習の入力データ | **禁止**（読み取り専用） |
| `units/<単元>/output/` | 演習の成果物 | ここにだけ書く |
| `units/<単元>/solution/` | 模範解答（一部の単元のみ） | 教材の改訂時のみ |
| `tools/` | 教材データの生成スクリプト | 教材の改訂時のみ |
| `site/` | 学習サイト（Artifact として公開する1枚のHTML） | 教材の改訂時のみ |
| `apps/rpa-lab/` | 単元13・14 で操作する練習用の業務システム | 教材の改訂時のみ |

## 演習中の作業ルール

- `units/*/data/` を変更・上書き・削除しない。壊したら `python3 tools/generate_all.py` で復元する。
  - 例外: 単元03（ファイル整理）は `data/受信箱/` を実際に動かす。
    作業前に `data/受信箱_backup/` へコピーしてから始める。
- ファイルを削除しない。不要なものは `_アーカイブ/` へ移動する。
- 推測でデータを埋めない。不明点は成果物に「要確認」として明示し、
  何が不明で誰に聞けばよいかを書く。
- 集計の元になる数字はソースを1つに固定し、どれを使ったかを成果物に書く。
- 相関を因果として書かない。因果を主張する場合は必要な追加データを明記する。
- 単元13・14（ブラウザ操作）では、認証情報をスクリプト・ログ・成果物に書かない。
  必ず環境変数から読む。練習用アカウントであっても同じ扱いにする。

## 教材を改訂するとき

- `units/*/data/` を手で編集しない。`tools/gen_*.py` を直して再生成する。
  生成スクリプトは乱数シードを固定してあるので、同じデータが再現される。
- 単元を追加したら、`site/index.html` の単元カードと `README.md` の一覧も更新する。
- 教材の文章は日本語。演習で使う架空の会社は「株式会社アルタイル」（製品 `Altair CRM`）で統一する。

## 環境

Python 3.11 / openpyxl / python-docx / python-pptx。出力は UTF-8、改行 LF。
単元13・14 のみ Node 20 以上（`apps/rpa-lab`、依存パッケージなし）と
Playwright（`pip install playwright && playwright install chromium`）を使う。
